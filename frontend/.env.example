# Optional: set when the frontend is deployed separately from the backend.
VITE_API_BASE_URL=

# Optional: customize the dev proxy target used by `npm run dev`.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# Required for AI Tutor chat (Groq API)
VITE_GROQ_API_KEY=
